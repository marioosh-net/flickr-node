<% for(var i=0; i<covers.length; i++) {%>
    <a url='/album/<%= covers[i].id %>' selector='#uu<%=i%>' href="#" title="<%= covers[i].title %>" class="openalbum pub" >
        <div id="uu<%=i%>" class='oneset oneset_<%= covers[i].id %> <%if(covers[i].id == photoset_id){%>album-selected<%}%>'>
            <%= covers[i].title %>
        </div>
     </a>
<% } %>